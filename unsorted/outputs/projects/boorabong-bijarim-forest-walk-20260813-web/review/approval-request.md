# 제작·비용 승인 요청서 (승인 대기)

- 프로젝트: `boorabong-bijarim-forest-walk-20260813`
- 작성일: 2026-08-13
- 상태: **승인 대기 — 유료 생성 미실행**

## 1. 스토리보드 선택 요청

담당자가 A / B / C 중 하나를 명시적으로 선택해야 다음 단계로 넘어간다.

| 안 | 콘셉트 | 시간 배분 | 카메라 성향 | 이 안을 고르는 경우 |
|---|---|---|---|---|
| **A안 기본형** | 대사와 상황을 명확히 전달 | 4 / 4 / 4 / 3초 | 측면·정면·로우앵글 풀샷 | 캐릭터를 처음 접하는 시청자에게 안정적으로 보여주고 싶을 때 |
| **B안 코미디형** | 표정·타이밍·정적으로 웃음을 만듦 | 3 / 3 / 5 / 4초 | 클로즈업·미디엄·로우앵글 와이드·바스트 | 요청하신 "즐거움·코믹"을 가장 강하게 밀 때 |
| **C안 감성형** | 제주 숲의 여백으로 세계관 분위기를 넓힘 | 5 / 4 / 4 / 2초 | 와이드·익스테블리싱·후면 와이드·하이앵글 와이드 | 브랜드 톤과 자연 배경을 강조할 때 |

세 안 모두 총 15초, 9:16, 부라봉 1인, 4장면이다.

## 2. 공식 레퍼런스

전 장면 공통으로 아래 3장을 레퍼런스 이미지로 첨부한다. 조립기가 자동으로 붙이며 사람이 바꾸지 않는다.

- `shared/references/pages/page-08.png` — 부라봉 소개
- `shared/references/pages/page-09.png` — 부라봉 턴어라운드
- `shared/references/pages/page-10.png` — 부라봉 사용 규칙

세계관 확인용으로 `page-03.png`을 함께 확인했다(프롬프트 첨부 대상은 아님).

## 3. 생성 규모

| 항목 | 값 |
|---|---|
| 선택 안의 생성 클립 수 | **4클립** (장면 4개) |
| 장면 이미지 수 | 선택 안 4장 (현재 3안 12장 모두 미생성) |
| 이미지 모델 | `nano_banana_pro` (가이드 `model_routing.character_image`) |
| 해상도 | 2k, 9:16 |
| 영상 모델 | **미정 — 담당자 확정 필요** |
| 재생성 상한 | **미정 — 담당자 확정 필요** (제안: 장면당 1회, 프로젝트 합계 3회) |

## 4. 예상 크레딧 — 확정 불가

**요청하신 입력만으로는 예상 크레딧을 계산할 수 없어 값을 채우지 않는다.**

- 현재 저장소에 Higgsfield 단가표가 없다. `character-guide.json`의 `model_routing`에도 크레딧 정보가 없다.
- 참고로 `boorabong-golebang-hallasan-hike-20260801` 프로젝트 이력에 *Kling v3.0 std 5클립 = 45 credits* 기록이 있으나, 이는 다른 모델·다른 프로젝트의 과거 기록이며 본 프로젝트의 단가로 확정할 수 없다.
- 이미지 생성(`nano_banana_pro`, 2k) 단가는 저장소에 기록이 없다.

담당자가 확정해야 할 값: **이미지 1장당 크레딧**, **영상 모델과 클립당 크레딧**, **재생성 상한**.

## 5. 현재 차단 사유

| # | 차단 항목 | 내용 |
|---|---|---|
| 1 | 장면 이미지 미생성 | 이 세션에서 `higgsfield` CLI 실행이 승인되지 않았다. 프롬프트 12건은 조립을 마쳤고 실행 명령까지 `storyboard/prompts/*.txt`에 저장되어 있다 |
| 2 | 자동 검수 실패 | 위 1번 때문에 `planning-review.md` 종합 판정이 실패다. 문서 검수 항목 자체는 전부 통과했다 |
| 3 | 스토리보드 미선택 | 담당자 선택 대기 |
| 4 | 예상 비용 미기록 | 4절 참조 |
| 5 | 제작·비용 미승인 | 승인 없이 유료 호출을 하지 않는다 |
| 6 | 앵커 컷 미기록 | `check-production-gate.mjs --stage video` 결과 `anchors`·`scene_images` 기록이 없다. 둘 다 장면 이미지가 생성되어야 채울 수 있으므로 1번이 풀리면 함께 해소된다 |

게이트 확인 명령과 결과:

```
node unsorted/scripts/check-production-gate.mjs \
  unsorted/outputs/projects/boorabong-bijarim-forest-walk-20260813/metadata/production-log.json \
  --stage video
→ 게이트 차단 (7개 위반). 영상 단계로 넘어가지 않는다.
```

## 6. 담당자에게 필요한 결정

1. 스토리보드 A / B / C 중 하나 선택
2. 비자림을 퐁당패밀리 배경으로 사용해도 되는지 확인 (공식 배경 목록 미확보)
3. 이미지·영상 크레딧 단가와 재생성 상한 확정
4. 배경음악 분위기, 게시 예정 플랫폼 확정 (사용자 입력 없음)
5. 위를 근거로 한 명시적 제작·비용 승인

승인이 기록되기 전까지 Higgsfield 유료 호출을 실행하지 않는다.
