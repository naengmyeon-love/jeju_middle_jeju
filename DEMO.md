# 해커톤 시연 대본

2026-08-11 리허설 실측 기준. 추정치가 아니라 실제로 돌려서 잰 값이다.

## 왜 이 구성인가

라이브로 전체 파이프라인을 돌리면 **무대에서 10분 넘게 침묵한다.** 실측값이다.

| 구간 | 실측 소요 |
|---|---|
| 실행 → 첫 로그 출력 | 수 초 |
| 1단계 기획안 완료 | 약 5분 |
| 3단계 시나리오 완료 | 약 8분 |
| 6단계까지 전체 완주 | **26분 19초** (이벤트 193건) |

완주 지점은 7단계 제작·비용 승인 앞이다. 승인이 없으므로 파이프라인이 스스로 멈춘다.

시연에 쓸 미리 돌려둔 프로젝트: `boorabong-bijarim-forest-walk-20260811-15s`
(기획안·시나리오·스토리보드 3안·자동 검수까지 완료, PNG 0장, 유료 호출 0건)

그래서 **라이브는 "시작되는 것"까지만** 보여주고, 결과물은 미리 돌려둔 프로젝트로 넘어간다.
유료 생성은 실행하지 않는다. 승인 게이트에서 막히는 것을 보여주는 쪽이 더 강한 그림이다.

## 시작 전 준비

터미널 2개.

```bash
npm run server
```

```bash
npm run web
```

`http://localhost:3000` 을 연다. 상단 바가 **초록 `● 서버 연결됨`** 인지 확인한다.
빨강 `● 가상 데이터 모드` 면 사이드카가 안 뜬 것이다. 이 상태로도 화면은 돌아가지만
실행 버튼이 열리지 않는다.

`PONGDANG_ALLOW_PAID` 는 **설정하지 않는다.** 상단 바에 `유료 생성 잠김` 이 떠야 한다.

### 사이드카를 시연 도중 재시작하지 않는다

실행 기록은 **메모리에만** 있다. 사이드카를 재시작하면 제작 이력의 "실제 실행 기록"이
0건이 되고, 진행 로그도 되찾을 수 없다. 디스크의 산출물 파일은 그대로 남으므로 4번
(결과물) 은 영향받지 않지만, 3번 라이브 실행을 다시 해야 한다.

5번(직접 고치기)의 저장 내용은 파일이라 재시작해도 남는다. 잃는 것은 실행 기록뿐이다.

시연 시작 전에 한 번 띄우고 끝날 때까지 그대로 둔다.

## 대본

### 1. 문제 제기 (30초)

> "캐릭터 IP 숏폼은 가이드를 지키는 게 전부입니다. 그런데 가이드는 PDF 26쪽이고,
> 사람이 매번 대조하지 않습니다."

실제로 있었던 일을 근거로 든다 — 이전 프로젝트에서 '부라봉 흰 하의 누락', '고르방 눈 누락'이
스토리보드에 적힌 채 재생성 없이 영상 생성까지 진행됐다.

### 2. 입력 (1분)

홈 → **＋ 새 프로젝트 체험** → 1단계.

보여줄 것: 주제·상황·감정·길이·캐릭터가 전부 **드롭다운**이다.

> "자유 입력이 없습니다. 화면이 보내는 건 키뿐이고 문장은 서버가 고릅니다.
> 화면이 문장을 만들어 보내면 그게 곧 원격 실행 경로가 되니까요."

캐릭터 칩을 4개 눌러 **3인에서 막히는 것**을 보여준다. 서버 규칙과 같은 제한이다.

### 3. 라이브 실행 (1분 30초)

**이 조건으로 파이프라인 실행** 을 누른다.

로그가 흐르기 시작한다:

```
상태   대기열 1번째
상태   실행 시작
시스템 init
진행   pongdang-pipeline 스킬을 호출해서 1단계부터 진행하겠습니다.
도구   Skill
도구   Bash / Read / Write ...
```

> "지금 실제로 Claude Code가 돌고 있습니다. 이 로그는 꾸민 게 아니라 SSE로 흘러오는
> 실시간 출력입니다."

**여기서 새로고침을 한 번 한다.** 로그가 그대로 복구되고, 로그 위에
`진행 중 · 17:54 시작 · 비자림 숲길 산책` 이 뜬다.

> "새로고침해도 복구됩니다. 브라우저에 저장한 게 아니라 서버가 진행 이력을 갖고 있어서,
> 다른 창으로 열어도 같은 실행이 보입니다."

실행이 끝난 뒤에 새로고침해도 마지막 실행이 남는다. 그때는 `마지막 실행` 으로 표시가 바뀐다.

전체가 끝날 때까지 기다리지 않는다. 다음으로 넘어간다.

### 4. 결과물 (1분 30초)

미리 돌려둔 프로젝트를 연다. 보여줄 것:

- `plan.md` — 제목에 부라봉 말투가 적용돼 있다: **"부라봉, 거인 나무 앞에 멈추다봉"**
- 미지정 항목은 **"담당자 확인 필요"** 로 남아 있다

> "배경·대사·BGM·플랫폼이 입력에 없었습니다. 지어내지 않고 확인 필요로 남깁니다.
> 이게 IP 작업에서 제일 중요합니다."

### 5. 담당자가 직접 고치기 (1분 30초)

4번의 "확인 필요"를 누가 채우는지로 자연스럽게 넘어간다.

**시작 전 확인**: 승인 탭에서 미리 돌려둔 프로젝트가 선택돼 있어야 한다. 저장 배지에
그 슬러그가 그대로 뜨므로, 4번에서 연 프로젝트와 같아야 말이 맞는다.

프로젝트 탭 → **4단계 시간대별 시나리오**. 항목마다 연필(✎)이 붙어 있다.

> "고칠 수 있는 자리에는 전부 연필이 있습니다. 표시가 없으면 담당자는 고칠 수 있다는
> 사실 자체를 모르고, 대사 한 줄 때문에 파이프라인을 26분 다시 돌립니다."

**장면 3 대사를 고친다.** `이걸로도 괜찮아!` → `이걸로도 괜찮다봉!`

저장하면 오른쪽 위 배지가 `<슬러그> 에 저장됨` 으로 바뀐다.

**여기서 새로고침을 한 번 한다.** 고친 대사가 그대로 남아 있다.

> "브라우저에 저장한 게 아닙니다. 프로젝트 폴더에 파일로 남습니다."

**검수와 연결된 것을 보여준다.** 6단계 기획 자동 검수로 간다. 방금 고치기 전에는
말투 위반으로 빨간 `필수 수정` 이었던 항목이 이렇게 바뀌어 있다:

```
수정 반영됨
장면 3 부라봉 말투 불일치
대사를 "이걸로도 괜찮다봉!" 으로 고쳤습니다. 재검수를 실행하면 이 항목은 해소됩니다.
```

> "검수가 지적한 그 문장을, 지적한 화면에서 바로 고칩니다."

#### 여기서 반드시 짚을 것 — 무엇을 건드리지 않는가

터미널에서 저장 파일을 열어 보여준다.

```bash
cat unsorted/outputs/projects/<슬러그>/storyboard/edits.json
```

```json
"history": [
  { "revision": 1, "field": "시나리오 장면 3 · 대사",
    "before": "이걸로도 괜찮아!", "after": "이걸로도 괜찮다봉!",
    "by": "콘텐츠 담당자", "at": "..." }
]
```

> "수정본은 **옆에 따로** 쌓입니다. 생성 산출물인 `storyboard-*.md` 도,
> 승인 게이트가 읽는 `production-log.json` 도 건드리지 않습니다."

이 점이 다음 순서(승인 게이트)로 그대로 이어진다.

> "즉 화면에서 글자를 고쳐서 승인을 통과시킬 수는 없습니다.
> 게이트가 읽는 파일에는 화면이 쓸 수 없으니까요."

시간이 남으면 창을 두 개 열어 같은 항목을 동시에 고쳐 본다. 늦게 누른 쪽이
`다른 곳에서 먼저 저장했습니다` 로 막히고, 화면이 먼저 저장된 내용으로 되맞춰진다.

### 6. 승인 게이트 (2분) ← 핵심

승인 탭 → 실제 승인 게이트.

세 단계가 **전부 빨간 차단**이다. 승인 요청을 눌러도 막힌다:

```
게이트 차단 — stage=video
  - anchors 기록이 없습니다. 등장 캐릭터마다 앵커 컷을 만들고 승인 기록을 남겨야 합니다.
  - scene_images 기록이 없습니다. 장면 컷마다 경로·모델·프롬프트·레퍼런스·대조 결과를 남겨야 합니다.
```

> "화면에서 승인 버튼을 눌렀다는 사실만으로는 통과하지 않습니다.
> 요청할 때마다 서버가 검사 스크립트를 다시 실행합니다."

역할을 담당자 → 팀장으로 바꿔 **최종 영상 승인이 그때 열리는 것**을 보여준다.

> "제작·비용, 최종 영상, 배포는 각각 별도의 결정입니다. 하나를 승인해도 다음이 안 열립니다."

### 7. 가이드 준수 강제 (1분 30초)

터미널에서 직접 보여준다. 실패했던 실제 입력이다.

```bash
node unsorted/scripts/build-pongdang-prompt.mjs --characters boo-rabong \
  --scene "BOO RABONG walks into the forest trail, then suddenly stops." \
  --camera "full shot tracking the character, transitioning to a face close-up"
```

거부된다:

```
[시간 흐름·연속 동작] --scene 안의 "then"
[카메라 이동·샷 전환] --camera 안의 "transitioning"
[샷이 2개 이상] --camera 안의 "close-up + full shot"
```

> "이 입력이 실제로 2패널 이미지를 만들었습니다. 카메라 지시가 2개면 패널이 2개,
> 3개면 3개가 나왔습니다. 6번 중 4번 상관이 정확했습니다."

`_rejected-v1.0/scene-03-a.png` (3패널)와 `scene-04-a.png` (정상)를 나란히 보여준다.

> "네거티브 문구로는 못 막았습니다. 이미지 모델은 긍정 지시가 부정 지시를 이깁니다.
> 그래서 통제 지점을 프롬프트가 아니라 입력으로 옮겼습니다."

**이어서 스토리보드를 연다** (`storyboard/storyboard-a.md`). 파이프라인이 스스로 둘을 분리해 놓았다.

```
| 카메라(영상 연출) | 정면 풀샷 고정 → 트래킹, 그늘 진입 시 살짝 줌인 |

- 카메라(정지 이미지용): `front-facing full shot` (앵글 1개 + 샷 크기 1개, 이동·전환 표현 없음)
- 정지 이미지 입력 게이트: 통과 (샷 1개 full shot, 시간 흐름·전환 표현 없음, 네거티브 16항목 포함)
```

> "영상용 지시는 그대로 남기고, 정지 이미지용은 단일 샷으로 다시 씁니다.
> 10장면 전부 게이트를 통과한 기록이 남습니다."

### 8. 마무리 (30초)

```bash
./verify.sh
```

> "안전장치가 고장 나면 파이프라인 전체가 무의미해집니다. 그래서 게이트를 검사하는
> 검사를 따로 뒀습니다."

37개 검사 통과를 보여주고 끝낸다.

## 사고 대응

| 증상 | 원인 | 대응 |
|---|---|---|
| 상단 바가 빨강 | 사이드카 꺼짐 | 화면은 목업으로 계속 돌아간다. 그대로 4번(결과물)으로 넘어간다 |
| 실행이 즉시 실패 | `claude` OAuth 만료 | 라이브 실행을 건너뛰고 4번부터 진행한다 |
| 로그가 안 흐름 | SSE 연결 문제 | 새로고침한다. 실행 중이면 자동으로 다시 붙는다 |
| 실행이 너무 오래 걸림 | 정상이다 | 기다리지 말고 4번으로 넘어간다. 취소하지 않아도 된다 |
| 저장 배지가 `이 화면에서만 유지됨` | 사이드카 꺼짐 | 고치는 것 자체는 된다. 5번의 새로고침 시연만 건너뛴다 |
| 저장 배지가 `저장 실패` | 다른 창에서 먼저 저장함 | 화면이 자동으로 최신본에 맞춰진다. 그대로 다시 고치면 저장된다 |

## 절대 하지 말 것

- `PONGDANG_ALLOW_PAID=1` 로 띄우지 않는다. 무대에서 돈이 나간다.
- 게이트를 통과시키려고 `production-log.json` 을 손으로 채우지 않는다.
  기록 위조이고, 게이트를 만든 이유 자체가 사라진다.
- 전체 파이프라인 완주를 라이브로 시도하지 않는다.
